# Tech Update: LLM Formatting and Rendering

## Changes Implemented

* **System Prompting:** Forced Gemini to return structured Markdown using headers, bold text, and bullet points.
* **Markdown Integration:** Installed `marked` to convert raw AI responses into formatted HTML.
* **UI Styling:** Added scoped CSS with `:deep()` selectors to correctly render bullet points and code blocks.
* **Function Scope Fix:** Defined `renderMarkdown` inside `<script setup>` to resolve template reference errors.

## Result

AI responses now render as properly styled, readable content with consistent formatting and no template runtime errors.